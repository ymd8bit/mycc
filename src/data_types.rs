enum Type {
  Int { nbits: usize, signed: bool }, // Float {
                                      //   nbits: usize,
                                      // }
                                      // Bool
                                      // Pointer(Box<Type>)
                                      // Ref(Box<Type>)
}
