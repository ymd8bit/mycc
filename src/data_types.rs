enum DataType {
  Int { nbits: usize, signed: bool }, // Float {
                                      //   nbits: usize,
                                      // }
                                      // Bool
                                      // Pointer(Box<DataType>)
                                      // Ref(Box<DataType>)
}
